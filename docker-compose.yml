version: "3"
services:
  mvf-service:
    container_name: mvf-service
    restart: always
    build:
      dockerfile: Dockerfile
      context: ./
    volumes:
      - ./src:/var/www/service
    command: php -S 0.0.0.0:8000 -t public
    ports:
      - "8000:8000"